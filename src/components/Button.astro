---
const { class: className, ...props } = Astro.props;
---

<a
    class:list={[
        "button grid grid-cols-1 grid-rows-1 w-fit",
        className,
    ]}
    {...props}
>
    <div
        class:list={["button__border-box", "row-start-1 col-start-1"]}
    >
    </div>
      <div class:list={["button__content", "row-start-1 col-start-1"]}><slot /></div></a>
